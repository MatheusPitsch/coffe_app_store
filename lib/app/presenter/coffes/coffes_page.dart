import 'package:flutter/material.dart';

class CoffesPage extends StatelessWidget {

  const CoffesPage({ Key? key }) : super(key: key);

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Cafes'),),
           body: Container(),
       );
  }
}